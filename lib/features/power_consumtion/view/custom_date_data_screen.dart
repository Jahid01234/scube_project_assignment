import 'package:flutter/material.dart';

class CustomDateDataScreen extends StatelessWidget {
  const CustomDateDataScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("BHJbhjj"),),
    );
  }
}
